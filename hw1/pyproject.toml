[tool.poetry]
name = "hw1-spmv-bench"
version = "0.1.0"
description = "SPMV benchmark and visualization utilities"
authors = ["PDP HW1"]
readme = "README_benchmark.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<3.14"
matplotlib = "^3.9.0"
pypdf = "^6.9.2"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
